///##################################################
///#### Generated by mvcBuilder - Do Not Modify #####
///##################################################
part of app_bar_module;

ControllerRegistry<AppBarController> _$registry = ControllerRegistry<AppBarController>();

class $AppBar extends ViewWrapper {
  const $AppBar({
    String? controllerId,
    bool forceAttach = false,
  }) : super(controllerId, forceAttach);
  factory $AppBar.fromController(
    AppBarController controller, {
    bool forceAttach = false,
  }) {
    return $AppBar(
      controllerId: controller.controllerId,
      forceAttach: forceAttach,
    );
  }
  @override
  State<StatefulWidget> createState() => _$AppBarState();
  static AppBarController createController({
    String? controllerId,
  }) {
    AppBarController controller = AppBarController._();
    _$registry.addController(controller, controllerId, () {});
    controller.onInit();
    return controller;
  }

  static AppBarController getController({required String id}) => _$registry.getControllerStrict(id);
  static AppBarController getControllerWhere(bool Function(AppBarController) callback) =>
      _$registry.getControllerWhereStrict(callback);
}

class _$AppBarState extends ViewWrapperState<$AppBar, AppBarController> {
  ControllerRegistry<AppBarController> registry = _$registry;
  View get view => AppBar._();
  AppBarController createController(String? controllerId) => $AppBar.createController(
        controllerId: controllerId,
      );
}

mixin _$AppBarControllerMixin on Controller {}
